## 1. 基础架构与云开发环境搭建

- [x] 1.1 初始化本地 `miniprogram` 和 `cloudfunctions` 项目，配置 `project.config.json` 中的云开发环境 ID
- [x] 1.2 创建微信云开发数据库集合 (Collections)：创建 `users`、`skincare_cabinet`、`skin_diary`、`buying_consultations` 集合，并配置对应权限
- [x] 1.3 设计并编写全局免责声明的 UI 弹窗，用户首次进入小程序时必须勾选确认，并将状态持久化在本地 LocalStorage 与 `users` DB 中

## 2. 核心功能 1：今日护肤 Routine (问卷与作战日历)

- [x] 2.1 编写“2分钟肤况问卷”的前端表单页面，包括肤质自评、敏感度、当前护肤目标、预算、生理期等选项并存入本地缓存
- [x] 2.2 在 `miniprogram` 编写 Routine 早晚生成规则引擎的核心算法（基于 JSON 决策树匹配护肤品柜中的单品类型，并在泛红时自动屏蔽违禁活性成分）
- [x] 2.3 编写“今日 Routine”首页 UI，实现晨间与夜间 Tab 切换展示、品类步骤时间线卡片以及左右横滑/双击轮替适配柜子同类单品的功能
- [x] 2.4 实现“一键护肤打卡”功能，用户打卡后将当日 Routine 所用产品 ID、打卡时间及评分参数打包写入 `skin_diary` 集合中

## 3. 核心功能 2：我的护肤品柜 (分类摆放与双通道入库)

- [x] 3.1 编写“我的护肤品柜”展示页面，实现以【洁面、水、精华、乳霜、猛药、防晒】为货架分层的精美网格卡片布局
- [x] 3.2 编写根据 `opened_date` 与 `pao_months` 动态计算 PAO 开封保质期进度条并渲染不同颜色（绿/黄/红过期警报）的前端展示逻辑
- [x] 3.3 编写“手动录入”页面，实现用户点击“+”通过胶囊组件选择品类、核心活性成分、开封时间与 PAO 保存入库
- [x] 3.4 编写“拍照扫描录入”云函数与前端页面，调用微信 OCR 提取包装文字，并喂给轻量 LLM (如 Gemini 3.5 Flash) 提取活性成分与保质期自动建档

## 4. 核心功能 3：皮肤日记 & 打卡与双图对比

- [x] 4.1 编写 30 秒极简打卡表单，实现油腻感、泛红、爆痘打分，记录睡眠与诱因（熬夜、奶茶、生理期）及拍照上传至微信云存储
- [x] 4.2 基于原生小程序 Canvas 编写“无滤镜对比画布（Before/After Slider）”组件，实现拖拽分界线拖动对比两张前后人脸照的无缝滑动动效
- [x] 4.3 编写“AI 趋势周报”云函数与前端，每满 5 次打卡读取日记 JSON 数据库并利用大模型分析本周吃辣熬夜与护肤品成分使用的皮肤关联趋势

## 5. 核心功能 4：小红书同款拔草冷静分析器

- [x] 5.1 编写小红书拔草详情页，支持用户拖拽上传图片或直接粘贴小红书笔记文案及详情链接
- [x] 5.2 编写“AI 避坑分析”云函数，利用 OCR 抓取名称并调用带有联网检索能力的大模型，结构化提炼新品的核心活性成分与宣称功效
- [x] 5.3 编写 AI 精准比对算法，将识别的新品成分与用户的【肤质档案】和【护肤柜已有产品】交叉判定，计算适合度、成分冲突及功重叠重合度
- [x] 5.4 编写毒舌闺蜜式分析书报告 UI，并支持将冷静拔草证书一键渲染为 9:16 精美海报卡片图下载保存到用户相册，用于小红书社交传播

## 6. 微信广告与合规过审防线

- [x] 6.1 在小程序中注册并挂载微信广告联盟“激励视频广告 (Rewarded Video Ads)”和“插屏广告”，实现拔草大模型分析与周报分析的广告置换免费阅读逻辑
  - ⚠️ 注：目前日记页面使用模拟广告倒计时代替真实 `wx.createRewardedVideoAd` API，拔草页暂无广告门控；需在正式上线前替换为真实广告单元 ID
- [x] 6.2 在云端/云函数层实现 Output 过滤拦截器，正则检测大模型生成结果，敏感词脱敏转换，确保 100% 绕过微信医疗化妆品功效审核红线
